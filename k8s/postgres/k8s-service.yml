apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: krm-kpmg
  labels:
    app: postgres-svc
spec:
  type: LoadBalancer
  ports: 
    - port: 5432
      targetPort: 5432
      nodePort: 30004
  selector: 
    app: postgresdb