# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
# value:\s*.*\s?
# Image registry
image:
  registry: ""  
default:
  registry: ""
appVersion: latest
images:
  eciRulesEngine:
    imageName: redis
    imageTag: alpine
