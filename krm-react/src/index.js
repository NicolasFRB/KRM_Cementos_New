import React from 'react';
import ReactDOM from 'react-dom/client';
import SelectControls from "./components/SelectControls";


const root = ReactDOM.createRoot(document.getElementById('select-controls'));
root.render(
  <SelectControls />
);
